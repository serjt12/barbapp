# Use Nginx image
FROM nginx:alpine

# Copy your local Nginx configuration file
COPY ./nginx.config.prod /etc/nginx/nginx.conf

# Copy static files to the Nginx html directory
COPY ../frontend/barbapp/build /usr/share/nginx/html

# Expose port 80
EXPOSE 80
